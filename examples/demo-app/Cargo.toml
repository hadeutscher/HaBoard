[package]
name = "demo-app"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
haboard = { path = "../../haboard", features = ["demo-scene"] }
winit = "0.30"
env_logger = "0.11"
